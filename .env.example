# Supabase Configuration
# Get these from your Supabase project settings: https://app.supabase.com/project/_/settings/api
VITE_SUPABASE_URL=your_supabase_project_url_here
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Application Mode
# Options: "dashboard" (standings view) or "prediction" (user input mode)
VITE_APP_MODE=dashboard

# Football Data API
# Get your API key from: https://www.football-data.org/
# Used by automation scripts to fetch real Premier League standings
FOOTBALL_API_KEY=your_football_api_key_here

# Discord Webhook (Optional)
# Create a webhook in your Discord server settings
# Used to send notifications when standings are updated
DISCORD_WEBHOOK_URL=your_discord_webhook_url_here

# Instructions:
# 1. Copy this file to .env.local
# 2. Replace all "your_*_here" values with your actual credentials
# 3. Never commit .env.local to git (it's already in .gitignore)
# 4. Keep your credentials secure and rotate them if exposed
